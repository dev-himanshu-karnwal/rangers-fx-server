/**
 * Common field types for DTO building
 */
export enum FieldType {
  EMAIL = 'email',
  PASSWORD = 'password',
  STRING = 'string',
  NUMBER = 'number',
  INTEGER = 'integer',
  ENUM = 'enum',
  BOOLEAN = 'boolean',
  DATE = 'date',
  USER_ID = 'userId',
  OTP = 'otp',
  TOKEN = 'token',
}
