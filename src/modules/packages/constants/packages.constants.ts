/**
 * Packages module constants
 */
export const PACKAGES_CONSTANTS = {
  /**
   * Bot expiration period in months
   * If no package is purchased with a bot within this period, the bot will expire
   */
  BOT_EXPIRATION_MONTHS: 3,
} as const;
