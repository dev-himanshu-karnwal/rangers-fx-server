/**
 * Auth module constants
 */

export const AUTH_CONSTANTS = {
  /**
   * Default JWT strategy name
   */
  DEFAULT_STRATEGY: 'jwt',

  /**
   * Password reset token expiration time in hours
   */
  RESET_TOKEN_EXPIRATION_HOURS: 1,  
} as const;

